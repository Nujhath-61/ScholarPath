import { Link, useNavigate, useParams } from "react-router-dom";
import "../styles/appPages.css";
export default function AdminScholarshipForm() { const { index } = useParams(); const navigate = useNavigate(); const items = JSON.parse(localStorage.getItem("list") || "[]"); const item = index === undefined ? null : items[Number(index)]; if (index !== undefined && !item) return <main className="content-page empty-page"><h1>Scholarship not found</h1><Link to="/admin">Back to dashboard</Link></main>; function submit(e) { e.preventDefault(); const value = Object.fromEntries(new FormData(e.currentTarget)); const next = [...items]; if (item) next[Number(index)] = { ...item, ...value }; else next.push(value); localStorage.setItem("list", JSON.stringify(next)); navigate("/admin"); } return <main className="auth-page"><section className="auth-card wide"><p className="eyebrow">ADMIN DASHBOARD</p><h1>{item ? "Edit scholarship" : "Post a scholarship"}</h1><p>Enter clear, complete information for students.</p><form onSubmit={submit}><label>Scholarship title<input required name="title" defaultValue={item?.title} /></label><div className="two-columns"><label>Location<input required name="location" defaultValue={item?.location} /></label><label>Deadline<input name="deadline" type="date" defaultValue={item?.deadline} /></label></div><div className="two-columns"><label>Study level<select name="type" defaultValue={item?.type || "Undergraduate"}><option>Undergraduate</option><option>Master&apos;s</option><option>PhD</option></select></label><label>Working experience<select name="hour" defaultValue={item?.hour || "Not required"}><option>Not required</option><option>1-2 Years</option><option>3-5 Years</option><option>5+ Years</option></select></label></div><label>Official link<input name="link" type="url" defaultValue={item?.link} /></label><label>Description<textarea required name="description" rows="7" defaultValue={item?.description} /></label><div className="form-actions"><Link className="secondary-button" to="/admin">Cancel</Link><button className="primary-button">{item ? "Save changes" : "Add scholarship"}</button></div></form></section></main>; }
